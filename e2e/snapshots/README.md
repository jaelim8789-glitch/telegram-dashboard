Place baseline screenshots here.

Naming convention:  {test-file-name}.spec.ts-snapshots/{describe-title}-{test-title}-{project}-{platform}.png

Generated automatically by:
  npx playwright test --update-snapshots --project=chromium visual-regression.spec.ts

If a baseline exists, subsequent runs compare against it.
If a test fails due to an intentional visual change (not a bug),
delete the old baseline and re-run with --update-snapshots.
