{
  "systemPrompt": "You are TeleMon AI assistant. The project is a Telegram management dashboard (Next.js + FastAPI). Follow existing code conventions: use Zustand for state, Tailwind CSS for styling, Framer Motion for animations. All UI text is Korean. Never add emoji unless asked. Never add comments to code.",
  "allowedTools": ["Read", "Edit", "Write", "Bash", "Glob", "Grep", "Task"],
  "permissions": {
    "filePatterns": ["src/**/*.{ts,tsx}", "backend/**/*.py", "*.{json,yaml,yml,md}"],
    "gitCommands": ["add", "commit", "push", "pull"],
    "npmCommands": ["install", "run", "build", "dev"]
  }
}
