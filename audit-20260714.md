# 텔레그램 대시보드 프로젝트 전체 감사 보고서
**일시**: 2026-07-14T10:25 KST  
**대상 브랜치**: `integrate/final-ui-ux-20260713`  
**검토 파일**: ~130개 (설정 14, 라우트 14, lib 14, store 2, types 1, components 74, i18n 4, e2e 9, 스크립트 1)

---

## 🔴 CRITICAL (즉시 조치 필요)

### 1. 보안: JWT 토큰을 `localStorage`에 저장
- 파일: `src/lib/auth.ts:3-9`, `src/lib/api.ts` (전반)
- 내용: XSS 공격 한 번으로 모든 사용자 토큰 탈취 가능. httpOnly+Secure+SameSite=Strict 쿠키 미사용.

### 2. 보안: CSRF 보호 전무
- 파일: 전체 API 호출 (api.ts, signup/page.tsx, admin/login/page.tsx 등 전반)
- 내용: 모든 상태 변경 API 호출에 CSRF 토큰이 없음.

### 3. PII(전화번호)가 Git에 일반 텍스트로 노출
- 파일: `fix_tenant.sql`
- 내용: `+18705212329`가 SQL 파일에 하드코딩되어 Git history에 영구 저장. GDPR 위반 가능.
- 조치: 즉시 .gitignore에 SQL 파일 추가 또는 git filter-repo로 history 정리

### 4. `useRecurringCountdown` Invalid Date 버그
- 파일: `src/lib/useRecurringCountdown.ts:21`
- 코드: `new Date(\`${iso}Z\`).getTime()`
- 내용: iso가 이미 Z로 끝나는 UTC 문자열이면 "2024-01-01T00:00:00ZZ" → Invalid Date → countdown에 NaN 표시
- 수정: `iso.endsWith('Z') ? iso : \`${iso}Z\``

### 5. E2E: `accounts.spec.ts` 첫 번째 테스트 무조건 실패
- 파일: `e2e/accounts.spec.ts:22-31`
- 내용: uniquePhone()으로 생성한 폰 번호를 등록하지 않고 DB에서 찾아 toBeTruthy() 검증 → 항상 실패

### 6. E2E: `p0-fixes.spec.ts` 경쟁 조건
- 파일: `e2e/p0-fixes.spec.ts:95-100, 123-134`
- 내용: page.goto() 후에 waitForResponse() 리스너 등록 → API 응답이 더 빨리 오면 timeout

### 7. `ko.json` 의미적 불일치 (semantic divergence)
- 파일: `src/messages/ko.json` - deliveryAnalytics.implementing
- 내용: en/ja/zh는 "Coming soon", ko는 "데이터가 없습니다. 발송 내역이 쌓이면 분석이 표시됩니다." (기능이 작동 중인 것처럼 오해)

### 8. `DOMAIN_CONNECTION_RUNBOOK.md` TLS 설정 모순
- 파일: `DOMAIN_CONNECTION_RUNBOOK.md`
- 내용: 제목/1단계는 "Full (Strict)", 본문은 "Full" 권장 → 운영자가 잘못 따라가면 526 오류로 사이트 장애

---

## 🟠 HIGH (심각)

### 9. error.tsx / loading.tsx 전무
- 전체 13개 라우트 중 0개가 에러 바운더리 또는 로딩 UI를 가짐

### 10. CSP(Content Security Policy) 미설정
- 파일: next.config.ts
- 내용: XSS 방어 계층 부재. dangerouslySetInnerHTML 사용 중인 layout.tsx와 함께 위험.

### 11. X-Powered-By: Next.js 헤더 노출
- 파일: next.config.ts - poweredByHeader: false 누락

### 12. Stripe sessionId UI 노출
- 파일: src/app/(public)/billing/success/page.tsx:96-99
- 내용: Stripe 내부 세션 ID를 사용자 화면에 표시

### 13. SendTab.tsx 1200라인 단일 컴포넌트
- 파일: src/components/workspace/SendTab.tsx
- 내용: 50+ useState, 여러 useEffect, 내부 컴포넌트 정의 포함

### 14. DashboardTab.tsx 내 RecurringCard 컴포넌트
- 파일: src/components/workspace/DashboardTab.tsx:60-98
- 내용: 컴포넌트가 DashboardTab 내부에 정의되어 매 렌더마다 unmount/remount

### 15. window.prompt() 사용
- 파일: src/components/workspace/SendTab.tsx:587
- 내용: 브라우저 네이티브 prompt() 스타일링 불가, 차단적

### 16. E2E broadcast.spec.ts — 테스트 이름과 본문 불일치
- 파일: e2e/broadcast.spec.ts:101-112
- 내용: "취소된 반복 발송" 테스트가 취소를 전혀 수행하지 않음

### 17. smoke_test.py Python 3.10+ 문법
- 파일: scripts/smoke_test.py:22
- 내용: str | None 타입 힌트 → Python 3.8/3.9에서 SyntaxError

### 18. useDashboardStore.getState() 직접 호출 패턴
- 파일: ProfileTab.tsx, GroupSearchTab.tsx, DashboardTab.tsx
- 내용: Zustand 구독 기반 업데이트를 우회

### 19. i18n.ts SSR 버그 — 전역 공유 상태
- 파일: src/lib/i18n.ts:17-23
- 내용: Node.js SSR에서 모듈 레벨 currentLocale 변수가 모든 요청 간 공유됨

### 20. PII 검색 URL 노출
- 파일: src/app/admin/support/page.tsx:160
- 내용: 전화번호 기반 검색 결과가 URL query param에 노출

---

## 🟡 MEDIUM (주의 필요)

### 21. target: ES2017 — 너무 낮은 버전 (tsconfig.json:3)

### 22. .gitignore 중복 항목 (.env* 2회, .vercel 2회)

### 23. Dockerfile HEALTHCHECK 누락

### 24. metadataBase 하드코딩 (layout.tsx, robots.ts, sitemap.ts)

### 25. features/page.tsx — 존재하지 않는 CSS 애니메이션 참조 (animate-slide-up)

### 26. 모든 컴포넌트 i18n 누락 (HeroSection.tsx 외 70+ 컴포넌트 한국어 하드코딩)

### 27. ChatDemo.tsx — 100ms setInterval 스크롤 (배터리/CPU 낭비)

### 28. ScrollProgress.tsx / ScrollIndicator.tsx — scroll 이벤트 throttling 누락

### 29. AdminGuard.tsx — useEffect deps 누락 (setSubscription)

### 30. GroupSearchInspector.tsx — 모든 API 에러 silent ignore

### 31. 중복 Input 컴포넌트 (ui/Field.tsx와 ui/Input.tsx)

### 32. useTheme.ts — localStorage 잘못된 값 타입 단언 (as Theme)

### 33. E2E playwright.config.ts — retries: 0

### 34. E2E 모든 테스트가 한국어 UI 텍스트에 의존

### 35. fix_tenant.sql — cooldown_minimum_minutes = 0 (rate limiting 비활성화)

### 36. LaunchOfferCountdown.tsx — 날짜 하드코딩 (2026-07-12/15)

---

## 🟢 LOW (참고 사항)

| # | 파일 | 문제 |
|---|------|------|
| 37 | package.json | engines 필드 누락 |
| 38 | next.config.ts | reactStrictMode 명시적 선언 누락 |
| 39 | eslint.config.mjs | 프로젝트 특화 규칙 전무 |
| 40 | features/page.tsx | prefers-reduced-motion 미고려 |
| 41 | Dockerfile | NEXT_PUBLIC_* ARG 기본값 빈 문자열 |
| 42 | 모든 admin 페이지 | formatDateTime "ko-KR" 하드코딩 |
| 43 | TabBar.tsx | role="tablist" 미사용 |
| 44 | DeliveryAnalyticsTab.tsx | P50/P95/P99 허구적 지표 (하드코딩 multiplier) |
| 45 | types/index.ts | snake_case vs camelCase 일관성 없음 |
| 46 | api.ts:323 | sendNowBroadcast URL 주석 불일치 |
| 47 | api.ts:765+ | days=0 falsy 처리로 쿼리 파라미터 누락 |
| 48 | api_group_search.ts:114 | 제네릭 타입 파라미터 누락 |
| 49 | helpers.ts | uniquePhone() counter > 99 시 전화번호 잘림 |
| 50 | smoke_test.py | 루트 / health check 누락, 기본 포트 불일치 |
