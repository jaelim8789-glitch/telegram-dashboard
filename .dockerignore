node_modules
.next
.git
.gitattributes
.gitignore
.tsbuildinfo*
report/
.pnpm-store

# CRITICAL: Root app/ and backend/ dirs shadow src/app for Next.js App Router.
# Present here for historical reasons -- they must NOT be in the Docker build context
# or the frontend build silently produces zero routes (all 404s).
/app
/backend
/telegram-dashboard-backend

.env*
!.env.example
npm-debug.log*
test-results
playwright-report
e2e
data/
sessions/
*.db
*.session
.backup
*.log
