// Sentry — client-side error tracking
// instrumentation.ts handles server-side init (Next.js canonical entry point)
// This file only provides the Sentry instance for client usage.

import * as Sentry from "@sentry/nextjs";

export default Sentry;
