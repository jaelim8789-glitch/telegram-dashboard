#!/bin/sh
set -e

echo "🔍 [post-checkout] 브랜치 전환 감지..."
CHANGED_FILES=$(git diff --name-only HEAD@{1} HEAD 2>/dev/null || true)
if echo "$CHANGED_FILES" | grep -q "package.json"; then
  echo "  package.json 변경 → npm install 실행"
  npm install --silent
fi
echo "✅ [post-checkout] 완료"
